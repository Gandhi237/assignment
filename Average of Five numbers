#include<stdio.h>
//Dis play the Average of 5 numbers
int main()
{
    float a, b, c, d, e, avg;
    printf("Enter five numbers\n");
    scanf("%f %f %f %f %f",&a, &b, &c, &d, &e);
    avg=(a+b+c+d+e)/5;
    printf("Average %f", avg);
    return 0;
}